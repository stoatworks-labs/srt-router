[package]
name = "crosspoint-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
bytes = "1"
tokio = { version = "1", features = ["sync"] }
parking_lot = "0.12"

[dev-dependencies]
tokio = { version = "1", features = ["sync", "macros", "rt"] }
